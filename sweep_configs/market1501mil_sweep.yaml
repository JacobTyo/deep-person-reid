program: scripts/CMIL_main.py
name: market1501mil
description: 'CMIL method on market1501mil'
project: CMIL-test2
method: bayes
metric:
  name: 'Test/market1501mil/rank1'
  goal: maximize
parameters:
  model_pretrained:
    values: [ True, False ]
  lr:
    distribution: log_uniform_values
    min: 0.00001
    max: 0.01
  fixbase_epoch:
    distribution: int_uniform
    min: 0
    max: 10
  dist_metric:
    values:
      - 'euclidean'
      - 'cosine'
  normalize_feature:
    values:
      - False
      - True
  weight_t:
    distribution: uniform
    min: 0.1
    max: 1.0
  weight_x:
    distribution: uniform
    min: 0
    max: 1.0
  weight_d:
    values: [ 0.0, 0.01, 0.1 ]
  margin:
    distribution: uniform
    min: 0.1
    max: 1.0
early_terminate:
  type: hyperband
  min_iter: 3
  eta: 2
command:
  - ${env}
  - ${interpreter}
  - ${program}
  - ${args}